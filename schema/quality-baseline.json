{
  "format": "codess.quality-baseline/1",
  "counts": {
    "lint": 173,
    "types": 80
  },
  "note": "Ceilings, not targets. A count at or below its entry passes; a rise fails. Lower an entry when a change reduces a count, with tools/quality_report.py --accept."
}
