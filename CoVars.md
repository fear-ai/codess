# CoVars

Every `CODESS_*` variable, how it is read, what sets it, and what bounds
it. Generated by `tools/variable_reference.py`; run that rather than
editing, and `--check` fails when the source has moved ahead of it.

## Resolution

**Flag, then variable, then built-in default**, each narrower than the
last: a flag names one invocation, a variable one shell, a default every
machine that never chose. `settings.resolve` applies it for a caller
holding an `argparse.Namespace`; `settings.resolve_named` for one holding
the value directly, which is what a leaf module that cannot import
`config` uses.

**A boolean composes rather than overrides.** `--force` with
`CODESS_FORCE=0` is force, and so is `CODESS_FORCE=1` with no flag: a
`store_true` flag cannot express *off*, so treating its absence as an
override would make the variable unsettable from any shell that also
passes flags.

## Bounds

**0 does not mean the same thing to every bound**, so each row states its
own. `disabled by 0` marks a bound whose consumer guards with
`if bound and ...`; `> 0` marks one whose consumer compares directly,
where 0 is the strictest possible value rather than none --
`SOURCE_READ_MAX` of 0 reads nothing, a `byte_limit` of 0 emits no rows.

## Variables

| Variable | Reader | Default | Flag | Bound |
|---|---|---|---|---|
| `CODESS_AGENT_KV` | `bool` | `'0'` | `--agent-kv` | -- |
| `CODESS_CC_PROJECTS` | `path` | `str(Path.home() / '.claude' / 'projects')` | `--` | absolute |
| `CODESS_CLAUDE_WORKTREES` | `bool` | `'0'` | `--` | -- |
| `CODESS_CONTENT_POLICY` | `str` | `None` | `--content-policy` | none |
| `CODESS_DAYS` | `int` | `365` | `--days` | 0 to 3650 |
| `CODESS_DEBUG` | `bool` | `'0'` | `--debug` | -- |
| `CODESS_FORCE` | `bool` | `'0'` | `--force` | -- |
| `CODESS_KEEP_SNAPSHOTS` | `int` | `3` | `--` | >= 0, disabled by 0 |
| `CODESS_MAX_CODESS_DB_BYTES` | `int` | `GB(2)` | `--` | > 0 |
| `CODESS_MAX_CONTEXT_CONTENT_CHARS` | `int` | `BUILTIN_MAXIMUMS['context_content_chars']` | `--` | > 0 |
| `CODESS_MAX_CURSOR_CONTAINER_BYTES` | `int` | `BUILTIN_MAXIMUMS['cursor_container_bytes']` | `--` | > 0 |
| `CODESS_MAX_CURSOR_DB_BYTES` | `int` | `GB(10)` | `--` | > 0 |
| `CODESS_MAX_EVENTS_PER_SESSION` | `int` | `BUILTIN_MAXIMUMS['events_per_session']` | `--` | > 0 |
| `CODESS_MAX_EVENTS_PER_SOURCE` | `int` | `BUILTIN_MAXIMUMS['events_per_source']` | `--` | > 0 |
| `CODESS_MAX_SCAN_DIRECTORIES` | `int` | `200000` | `--max-directories` | >= 0, disabled by 0 |
| `CODESS_MAX_TRANSCRIPT_BYTES` | `int` | `BUILTIN_MAXIMUMS['transcript_bytes']` | `--` | > 0 |
| `CODESS_MIN_SIZE` | `int` | `KB(8)` | `--min-size` | >= 0 |
| `CODESS_NO_CONTRACT_CHECK` | `bool` | `'0'` | `--no-check` | -- |
| `CODESS_NO_HASH` | `bool` | `'0'` | `--no-hash` | -- |
| `CODESS_QUERY_BYTE_LIMIT` | `int` | `MB(16)` | `--` | > 0 |
| `CODESS_RAW_MODE` | `raw_mode` | `'reference'` | `--raw-mode` | one of the declared modes |
| `CODESS_REDACT` | `bool` | `'0'` | `--redact` | -- |
| `CODESS_RESOURCE_POLICY` | `str` | `None` | `--resource-policy` | none |
| `CODESS_SCAN_TIMEOUT` | `int` | `900` | `--scan-timeout` | >= 0, disabled by 0 |
| `CODESS_SOURCE_READ_MAX` | `int` | `MB(64)` | `--` | > 0 |
| `CODESS_STOP` | `bool` | `'0'` | `--stop` | -- |
| `CODESS_STORE_ROOT` | `expanded_path` | `str(Path.home() / '.codess')` | `--store` | absolute; rejects `.`, `..`, `""` |
| `CODESS_STRICT_MAPPING` | `bool` | `'0'` | `--strict-mapping` | -- |
| `CODESS_SUBAGENT` | `bool` | `'0'` | `--subagent` | -- |
| `CODESS_VERBOSE` | `bool` | `'0'` | `--verbose` | -- |

## Read Outside the Table

These are read by a module that cannot import `config` without a cycle,
so they do not appear in `_IS_ENV_TABLE` and are listed here instead.
Absent from the generated table is not the same as absent from the
program, which is what this section exists to say.

| Variable | Read by | Supplies |
|---|---|---|
| `CODESS_DISCOVERY_POLICY` | `helpers` | A replacement discovery policy file |
| `CODESS_EXCLUDE_PATHS` | `helpers` | Trees on this machine that are not the operator's work; ships empty |
| `CODESS_INCLUDE_PATHS` | `helpers` | Trees admitted despite a rule that would skip them; outranks every other rule |
| `CODESS_CONFIG` | `settings` | The configuration file, which loses to a variable and wins over a built-in |
| `CODESS_NO_HASH` | `fileio` | Declared leaf-visible; `apply_leaf_visible` writes it from the flag |
| `CODESS_NO_CONTRACT_CHECK` | `schema_contract` | Declared leaf-visible, as above |
